# Webhook Discord pour le formulaire de contact (cf. app/api/contact/route.ts).
# Récupéré dans Discord : Paramètres du salon → Intégrations → Webhooks.
# /!\ Cette URL est un secret — ne JAMAIS la commit publiquement.
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/XXXXXXXXXXXXX/yyyyyyyyyyyy

# URL publique du site (utilisée par sitemap, robots, OG).
# Optionnelle en dev, à définir en prod.
# NEXT_PUBLIC_SITE_URL=https://jeremynowak.dev
